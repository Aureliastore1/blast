/**
 * Reserved for shared types/constants between apps/api and apps/web
 * (e.g. campaign status enums, API response shapes) in a future iteration.
 * Currently unused — see docs/ROADMAP.md.
 */
export {};
